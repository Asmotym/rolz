services:
  mysql:
    image: mysql:8.0
    container_name: rolz-mysql
    env_file:
      - .env
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-rolz}
      MYSQL_USER: ${MYSQL_USER:-rolz}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-rolz}
      MYSQL_TCP_PORT: ${MYSQL_PORT:-3306}
    ports:
      - "${MYSQL_PORT:-3306}:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    command:
      - --default-authentication-plugin=mysql_native_password
    restart: unless-stopped

  phpmyadmin:
    image: phpmyadmin:latest
    depends_on:
      - mysql
    env_file:
      - .env
    environment:
      PMA_HOST: mysql
      PMA_PORT: ${MYSQL_PORT:-3306}
      UPLOAD_LIMIT: 64M
    ports:
      - "8080:80"

  rolz:
    build:
      context: .
      target: runner
    image: rolz-all:latest
    container_name: rolz-stack
    depends_on:
      - mysql
    env_file:
      - .env
    environment:
      MYSQL_USER: ${MYSQL_USER:-rolz}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-rolz}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-rolz}
      MYSQL_PORT: ${MYSQL_PORT:-3306}
      ROLZ_DEV_MODE: ${ROLZ_DEV_MODE:-true}
      BACKEND_PORT: ${BACKEND_PORT:-4000}
      FRONTEND_PORT: ${FRONTEND_PORT:-5173}
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost:5173}
      VITE_BACKEND_URL: ${VITE_BACKEND_URL:-http://localhost:4000}
      VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://api.rolz.asmotym.fr}
      VITE_API_DOCS_URL: ${VITE_API_DOCS_URL:-http://localhost:6000}
      DATABASE_URL: ${DATABASE_URL:-mysql://rolz:rolz@mysql:3306/rolz}
      DATABASE_SSL: ${DATABASE_SSL:-false}
      VIRTUAL_HOST: rolz.asmotym.fr
      VIRTUAL_PORT: 5173
      LETSENCRYPT_HOST: rolz.asmotym.fr
      LETSENCRYPT_EMAIL: ${LE_EMAIL}
    ports:
      - "${FRONTEND_PORT:-5173}:5173"
      - "${BACKEND_PORT:-4000}:4000"
    volumes:
      - ./package.json:/app/package.json
      - ./package-lock.json:/app/package-lock.json
      - ./vite.config.ts:/app/vite.config.ts
      - ./tsconfig.json:/app/tsconfig.json
      - ./tsconfig.app.json:/app/tsconfig.app.json
      - ./tsconfig.node.json:/app/tsconfig.node.json
      - ./tsconfig.server.json:/app/tsconfig.server.json
      - ./tsup.config.ts:/app/tsup.config.ts
      - ./index.html:/app/index.html
      - ./src:/app/src
      - ./server:/app/server
      - ./public:/app/public
      - ./docker/entrypoint.sh:/app/docker/entrypoint.sh
    command:
      - /bin/bash
      - /app/docker/entrypoint.sh
    restart: unless-stopped

  api-docs:
    image: swaggerapi/swagger-ui:latest
    container_name: rolz-api-docs
    environment:
      PORT: 6000
      API_BASE_URL: ${API_BASE_URL:-https://api.rolz.asmotym.fr}
      SWAGGER_JSON: /tmp/rolz-api.yaml
      VIRTUAL_HOST: docs.rolz.asmotym.fr
      VIRTUAL_PORT: 6000
      LETSENCRYPT_HOST: docs.rolz.asmotym.fr
      LETSENCRYPT_EMAIL: ${LE_EMAIL}
    command:
      - sh
      - -c
      - sed "s|__API_BASE_URL__|$${API_BASE_URL}|g" /openapi/rolz-api.template.yaml > /tmp/rolz-api.yaml && /docker-entrypoint.sh nginx -g 'daemon off;'
    ports:
      - "${API_DOCS_PORT:-6000}:6000"
    volumes:
      - ./docs/openapi.template.yaml:/openapi/rolz-api.template.yaml:ro
    restart: unless-stopped

volumes:
  mysql-data:
